interface persona {
    vnombyap: string;
    vtitulo: string;
    vlugar: [string, string];
}

interface expLaboral {
    vpuesto: string;
    vempresa: string;
    vtiempo: [string, string];
    vdescripcion: string;
}

interface estudio {
    vlugar: string;
    vtiempo: [string, string];
    vtitulo: string;
}